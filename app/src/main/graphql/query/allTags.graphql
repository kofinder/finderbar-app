query AllTagsQuery($criteria: Criteria!){
  allTags(criteria: $criteria) {
    tags {
      tagName
      tagCount
    }
    hasNext
    totalCount
    error {
      status
      statusCode
      message
    }
  }
}
